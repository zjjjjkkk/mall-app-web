# Mall App Web - 电商移动端应用 v2.0

这是一个基于 uni-app 开发的跨平台电商应用项目，支持多端运行（微信小程序、H5、App等）。提供完整的电商购物功能，包括商品浏览、购物车、订单管理、支付、会员中心等核心功能。

## 📋 版本更新日志

### v2.0.0 (2025-12-09) - 重大功能更新

#### 🎉 核心新增功能

**1. UGC内容社区（广场）**
- ✅ 帖子发布：支持文本、图片、视频上传
- ✅ 帖子浏览：广场页面，支持下拉刷新、上拉加载
- ✅ 点赞功能：帖子点赞/取消点赞
- ✅ 评论系统：评论、回复、评论点赞
- ✅ 媒体上传：图片/视频上传到OSS（支持大文件，动态超时）

**2. 消息通知系统**
- ✅ 通知列表：订单、评论、点赞、回复通知
- ✅ 未读提醒：用户中心显示未读消息数量徽章
- ✅ 消息管理：标记已读、全部已读

**3. 搜索优化**
- ✅ 搜索建议：输入时实时显示搜索建议
- ✅ 搜索历史：本地+云端同步的搜索历史
- ✅ 热门搜索：显示热门搜索关键词

**4. 商品报修功能**
- ✅ 报修申请：支持邮寄/线下到店两种方式
- ✅ 地图导航：集成高德地图，显示距离和时间
- ✅ 门店选择：根据品牌自动匹配门店
- ✅ 支付流程：支付维修费、确认收货

**5. 个人资料管理**
- ✅ 资料编辑：用户名、性别、电话、个性签名
- ✅ 头像上传：支持头像上传到OSS

**6. 在线客服**
- ✅ 智能客服：关键词识别，自动回复
- ✅ 报修入口：识别"报修"关键词，快速申请

**7. 签到功能**
- ✅ 每日签到：签到获得10积分
- ✅ 签到记录：查看签到历史和连续天数

**8. UI优化**
- ✅ 登录/注册页面：现代化UI设计，渐变背景
- ✅ 广场页面：卡片式设计，智能媒体布局
- ✅ 发布页面：短视频平台风格UI

**9. 积分与成长值系统（新增）**
- ✅ 积分与成长值页面：统一的积分和成长值展示页面
- ✅ 积分流水：积分增减明细，支持升序/倒序排序
- ✅ 成长值明细：成长值变动记录，显示等级变化
- ✅ 时间筛选：支持按时间范围筛选历史记录
- ✅ 会员权益：会员权益信息显示在用户标识处
- ✅ 会员类型标识：在帖子中显示会员类型（月度/季度/年度会员，不同颜色）

**10. 商品对比功能（新增）**
- ✅ 添加对比：商品详情页支持添加商品到对比列表
- ✅ 对比页面：横向对比表格，支持左右滑动查看多个商品
- ✅ 对比维度：价格、品牌、销量、库存、商品参数（屏幕、处理器、内存、存储、摄像头、电池等）
- ✅ 对比管理：最多添加10个商品，支持删除、清空操作

#### 🔧 技术优化

- ✅ 大文件上传优化（支持500MB视频）
- ✅ 跨平台兼容性优化（H5/小程序/App）
- ✅ 网络请求优化（统一错误处理）
- ✅ 性能优化（懒加载、缓存）

---

### v1.0.0 - 基础版本

包含基础电商功能：
- 商品浏览、购物车、订单管理
- 用户登录/注册
- 支付功能
- 地址管理

## 项目简介
Mall App Web 是基于 uni-app 的移动端电商应用，覆盖商品、购物车、订单、支付、会员、退款、报修、搜索、UGC、通知等完整链路，支持 H5/小程序/App 多端运行。

## 技术栈分析（选型与缘由）
- uni-app + Vue2：一套代码多端复用，生态成熟；Vue2 轻量易上手。
- Vuex：统一管理用户、购物车、积分、消息等全局状态。
- luch-request/Axios 封装：统一请求与错误处理，便于跨端适配。
- ECharts：数据可视化（如积分趋势、统计）。
- 阿里云 OSS：媒体/头像/帖子图片视频存储。
- 高德地图 SDK：报修门店定位与导航。
- JWT：与后端统一的无状态认证。

## 整体设计分析
- 物理结构：`api/` 请求封装，`pages/` 业务页面，`components/` 公共组件，`store/` 全局状态，`utils/` 工具方法。
- 逻辑结构：页面 → API 封装 → 后端接口；Vuex 贯穿登录态、用户信息、购物车、消息、积分。
- 模块划分：商品/搜索、购物车、订单/退款、会员中心、报修、UGC、通知、客服、签到、个人资料。

## 代码实现要点（本版新增与关键）
- 搜索体验：新增分类筛选、综合/新品/销量/价格排序，参数直连后端 ES 搜索；保留历史/热门/建议。
- 积分一致性：登录/启动刷新用户信息，签到后清缓存，Vuex 合并本地与服务端积分，防止重登丢失。
- UGC 与推荐：帖子详情、商品详情展示知识图谱相关推荐，含推荐理由与分数。
- 报修增强：邮寄到商家需填写快递公司/单号；地图导航与品牌门店匹配。
- 通知与消息：未读徽章、列表、标记已读；客服关键词识别。

## ✨ 核心特性

- 📱 **跨平台支持**：一套代码，多端运行（H5、微信小程序、App）
- 🎨 **现代化UI设计**：优化的首页界面，便于用户浏览
- 🛒 **完整购物流程**：商品浏览 → 加入购物车 → 下单 → 支付
- 💰 **多种支付方式**：支持支付宝、微信支付
- 🎁 **积分系统**：每日签到获得积分，积分可兑换优惠券
- 📦 **分类优化**：两级分类展示，分类下直接显示商品
- 🔄 **退款退货**：支持退款申请、退货申请
- 📍 **定位服务**：支持获取用户位置信息（显示省市）
- 🔧 **商品报修**：支持邮寄/线下到店报修，集成高德地图导航，支付维修费，确认收货（新增）
- 🔍 **商品搜索**：完整搜索功能，支持搜索历史、热门搜索、搜索建议（新增）
- 💬 **在线客服**：智能客服系统，关键词识别，报修申请入口（新增）
- 📝 **UGC内容**：帖子发布、浏览、点赞、评论、回复，支持图片/视频上传（新增）
- 🔔 **消息通知**：订单通知、评论通知、点赞通知，未读消息提醒（新增）
- 👤 **个人资料**：用户名、性别、电话、个性签名、头像编辑（新增）
- ⭐ **积分系统**：积分流水查询、排序、时间筛选，积分与成长值统一管理（新增）
- 📈 **成长值系统**：成长值历史记录、等级变化追踪（新增）
- 👑 **会员权益**：月度/季度/年度会员，积分倍率加成，会员类型标识（新增）
- 🔍 **商品对比**：多商品横向对比，参数对比，辅助购买决策（新增）

## 技术栈

- **框架**: uni-app 3.0.0
- **开发语言**: Vue.js 2.6.14
- **网络请求**: luch-request 3.0.0 (封装的 uni.request)
- **状态管理**: Vuex
- **构建工具**: @vue/cli-service 4.5.15
- **UI组件**: uni-app 官方组件 + 自定义组件

## 📁 项目结构

```
mall-app-web/
├── api/                    # API接口目录
│   ├── address.js          # 地址管理API
│   ├── brand.js            # 品牌管理API
│   ├── cart.js             # 购物车API
│   ├── coupon.js           # 优惠券API（含积分兑换）
│   ├── home.js             # 首页API
│   ├── member.js           # 会员API（含个人资料、头像上传）
│   ├── order.js            # 订单管理API
│   ├── product.js          # 商品管理API
│   ├── productCompare.js  # 商品对比API（新增）
│   ├── refund.js           # 退款相关API
│   ├── returnApply.js      # 退货相关API
│   ├── repair.js           # 报修相关API（含支付、确认收货）
│   ├── signIn.js           # 签到API
│   ├── ugc.js              # UGC帖子API（新增）
│   ├── notification.js     # 消息通知API（新增）
│   ├── search.js           # 搜索API（新增）
│   └── pointsGrowth.js     # 积分与成长值API（新增）
├── components/             # 组件目录
│   ├── empty.vue           # 空状态组件
│   ├── mix-list-cell.vue   # 列表单元格组件
│   ├── mix-loading/        # 加载组件
│   ├── share.vue           # 分享组件
│   ├── uni-load-more/      # 加载更多组件
│   ├── uni-number-box.vue  # 数字输入框组件
│   └── upload-images.vue   # 图片上传组件
├── pages/                  # 页面目录
│   ├── index/              # 首页
│   │   └── index.vue       # 首页（已优化UI、定位显示）
│   ├── category/           # 分类页面
│   │   └── category.vue    # 分类页面（已优化，支持商品展示）
│   ├── search/             # 搜索页面（新增）
│   │   └── search.vue     # 搜索页面（搜索历史、热门搜索、搜索建议）
│   ├── repair/             # 报修页面（新增）
│   │   ├── apply.vue       # 报修申请页面（邮寄/到店、地图导航、门店选择）
│   │   ├── list.vue        # 报修列表页面
│   │   └── detail.vue      # 报修详情页面（含支付、确认收货）
│   ├── ugc/                # UGC帖子页面（新增）
│   │   ├── list.vue        # 帖子列表页面
│   │   ├── detail.vue      # 帖子详情页面
│   │   └── publish.vue     # 发布帖子页面（支持图片/视频上传）
│   ├── product/            # 商品相关页面
│   │   ├── product.vue     # 商品详情页面（含加入对比功能）
│   │   ├── compare.vue    # 商品对比页面（新增）
│   │   ├── list.vue        # 商品列表页面
│   │   ├── hotProductList.vue  # 热销商品列表
│   │   └── newProductList.vue # 新品列表页面
│   ├── cart/               # 购物车页面
│   │   └── cart.vue        # 购物车页面
│   ├── order/              # 订单管理页面
│   │   ├── order.vue       # 订单列表页面
│   │   ├── orderDetail.vue # 订单详情页面
│   │   ├── createOrder.vue # 创建订单页面
│   │   ├── refundApply.vue # 退款申请页面
│   │   └── returnApply.vue # 退货申请页面
│   ├── user/               # 用户相关页面
│   │   ├── user.vue        # 用户中心页面（含签到功能、个人资料入口、消息通知入口）
│   │   ├── customerService.vue  # 在线客服页面（新增）
│   │   ├── refundList.vue  # 退款列表页面
│   │   ├── brandAttention.vue  # 品牌关注页面
│   │   ├── productCollection.vue # 商品收藏页面
│   │   └── readHistory.vue # 阅读历史页面
│   ├── notification/       # 消息通知页面（新增）
│   │   └── notification.vue  # 消息通知列表页面
│   ├── userinfo/           # 个人资料页面（新增）
│   │   └── userinfo.vue    # 个人资料编辑页面（用户名、性别、电话、签名、头像）
│   ├── points/             # 积分与成长值页面（新增）
│   │   └── points.vue       # 积分与成长值页面（积分流水、成长值明细、排序、时间筛选）
│   ├── address/            # 地址管理页面
│   ├── coupon/             # 优惠券页面
│   ├── brand/              # 品牌页面
│   ├── money/              # 支付相关页面
│   └── public/             # 公共页面（登录、注册）
├── static/                 # 静态资源目录
├── store/                  # 状态管理目录
│   └── index.js            # Vuex状态管理
├── utils/                  # 工具函数目录
│   ├── appConfig.js        # 应用配置（API地址等）
│   ├── config.js           # 配置文件（API Key等，新增）
│   ├── date.js             # 日期处理工具
│   ├── errorHandler.js    # 错误处理工具（新增）
│   ├── imageUtil.js        # 图片处理工具（新增）
│   ├── logger.js           # 统一日志工具（新增）
│   ├── request.js          # 请求相关工具
│   └── requestUtil.js      # 请求工具封装（luch-request）
├── App.vue                 # 应用入口文件
├── main.js                 # 应用入口js文件
├── manifest.json           # 应用配置文件
├── pages.json              # 页面路由配置
└── uni.scss                # uni-app全局样式文件
```

## 🎯 功能模块详解

### 1. 首页模块

**功能特性**：
- **优化后的UI设计**：
  - 渐变背景的搜索栏
  - 轮播图展示（支持指示器）
  - 快捷功能区（分类、优惠券、品牌、新品）
  - 模块化卡片设计
- **内容展示**：
  - 品牌制造商直供（横向滚动）
  - 限时秒杀专区（含倒计时）
  - 新鲜好物（横向滚动）
  - 人气推荐（列表式布局）
  - 猜你喜欢（网格布局）
- **交互功能**：
  - 下拉刷新
  - 上拉加载更多
  - 定位服务

**页面文件**：`pages/index/index.vue`

### 2. 分类模块

**功能特性**：
- **两级分类展示**：
  - 左侧：一级分类列表
  - 右侧：二级分类 + 商品列表
- **商品展示**：
  - 分类下直接显示商品
  - 商品卡片展示（图片、名称、价格、销量）
  - 支持点击商品跳转详情
- **交互优化**：
  - 自动选中第一个二级分类
  - 切换分类自动加载商品
  - 上拉加载更多商品

**页面文件**：`pages/category/category.vue`

### 3. 商品模块

**功能特性**：
- **商品列表**：
  - 支持搜索、筛选
  - 支持排序（综合、销量、价格）
  - 分页加载
- **商品详情**：
  - 商品图片轮播
  - 商品基本信息
  - 商品规格选择（SKU）
  - 商品详情（富文本）
  - 加入购物车
  - 立即购买

**页面文件**：
- `pages/product/list.vue` - 商品列表
- `pages/product/product.vue` - 商品详情
- `pages/product/hotProductList.vue` - 热销商品
- `pages/product/newProductList.vue` - 新品列表

### 4. 购物车模块

**功能特性**：
- 购物车商品列表
- 商品数量修改
- 商品删除
- 商品勾选/全选
- 购物车结算
- 促销计算（优惠券、满减）

**页面文件**：`pages/cart/cart.vue`

### 5. 订单模块

**功能特性**：
- **订单创建**：
  - 选择收货地址
  - 选择优惠券
  - 选择支付方式
  - 订单金额计算
- **订单管理**：
  - 订单列表（待付款、待发货、待收货、已完成）
- 订单详情
  - 订单取消
  - 确认收货
- **退款退货**：
- 退款申请
- 退货申请
  - 退款/退货进度查询

**页面文件**：
- `pages/order/createOrder.vue` - 创建订单
- `pages/order/order.vue` - 订单列表
- `pages/order/orderDetail.vue` - 订单详情
- `pages/order/refundApply.vue` - 退款申请
- `pages/order/returnApply.vue` - 退货申请

### 6. 用户中心模块

**功能特性**：
- **用户信息**：
  - 用户头像、昵称
  - 积分、成长值显示
  - 优惠券数量
- **每日签到**（新增功能）：
  - 签到卡片UI
  - 每日签到获得10积分
  - 连续签到天数显示
  - 签到状态提示
- **订单管理**：
  - 全部订单
  - 待付款
  - 待收货
  - 退款/售后
- **个人中心**：
  - 地址管理
  - 我的足迹（浏览历史）
  - 我的关注（品牌）
  - 我的收藏（商品）
  - 我的退款
  - 设置

**页面文件**：`pages/user/user.vue`

### 14. 签到功能

**功能说明**：用户每日签到可获得积分，积分可用于兑换优惠券。

**API接口**（`api/signIn.js`）：
- `signIn()` - 用户签到
- `checkTodaySignIn()` - 查询今日是否已签到
- `getRecentSignInList(days)` - 查询最近N天签到记录
- `getCurrentMonthSignInList()` - 查询本月签到记录
- `getContinuousSignInDays()` - 查询连续签到天数

**积分规则**：
- 每次签到获得：10积分
- 积分兑换优惠券：1元 = 10积分

**UI展示**：
- 签到卡片（渐变背景）
- 连续签到天数显示
- 签到按钮（已签到状态禁用）

### 15. 积分兑换优惠券

**功能说明**：用户可以使用积分兑换优惠券。

**API接口**（`api/coupon.js`）：
- `exchangeCouponWithIntegration(couponId)` - 使用积分兑换优惠券

**兑换规则**：
- 优惠券金额 × 10 = 所需积分
- 例如：10元优惠券需要100积分

### 16. 优惠券模块

**功能特性**：
- 优惠券列表
- 优惠券领取
- 我的优惠券（未使用、已使用、已过期）
- 积分兑换优惠券

**页面文件**：`pages/coupon/couponList.vue`

### 10. 地址管理模块

**功能特性**：
- 地址列表
- 地址添加、编辑、删除
- 默认地址设置
- 高德地图选点（H5平台）

**页面文件**：
- `pages/address/addressManage.vue` - 地址列表
- `pages/address/address.vue` - 地址编辑

### 11. 商品报修模块（新增）

**功能特性**：
- **报修申请**：
  - 先选择门店，再选择报修方式（邮寄/到店）
  - 根据商品品牌自动匹配门店列表
  - 支持门店评分、地址、营业时间展示
  - 集成高德地图（显示商家位置、距离、时间）
  - 支持高德地图导航（APP端使用高德地图APP）
  - 图片上传功能
  - 自动计算距离和预计时间
- **报修管理**：
  - 报修列表查看
  - 报修详情查看（含支付、发货、确认收货状态）
  - 取消报修
- **报修支付流程**（新增）：
  - 维修完成后，用户支付200元维修费并提交收货地址
  - 管理员发货后，用户可确认收货
  - 支持支付状态、发货信息、物流单号展示

**页面文件**：
- `pages/repair/apply.vue` - 报修申请页面（含门店选择）
- `pages/repair/list.vue` - 报修列表页面
- `pages/repair/detail.vue` - 报修详情页面（含支付、确认收货）

**API接口**（`api/repair.js`）：
- `createRepair()` - 创建报修申请
- `getMyRepairList()` - 获取我的报修列表
- `getRepairDetail()` - 获取报修详情
- `cancelRepair()` - 取消报修
- `payRepair()` - 支付维修费并提交收货信息（新增）
- `confirmReceive()` - 确认收货（新增）
- `listBrandStores()` - 根据品牌名获取门店列表（新增）

### 12. 商品搜索模块（新增）

**功能特性**：
- **搜索功能**：
  - 关键词搜索
  - 搜索历史（本地存储，最多10条）
  - 热门搜索（预设关键词）
  - 搜索建议（基于历史记录）
  - 搜索结果展示（分页加载）
- **交互优化**：
  - 输入框适配屏幕（支持安全区域）
  - 下拉刷新
  - 上拉加载更多

**页面文件**：`pages/search/search.vue`

**API接口**（`api/product.js`）：
- `searchProductList()` - 商品搜索接口

### 13. 在线客服模块（新增）

**功能特性**：
- **客服功能**：
  - 提交反馈
  - 在线客服（智能客服）
  - 关键词识别（报修、退款、退货、物流、优惠、发票）
  - 自动发送欢迎语和预设问题
  - 报修申请入口（识别"报修"关键词，自动获取最近订单）
- **交互优化**：
  - 输入框适配屏幕
  - 消息列表自动滚动
  - 预设问题快速选择

**页面文件**：`pages/user/customerService.vue`

**API接口**：
- `api/order.js` - `getLatestOrder()` - 获取最近一次订单
- `api/repair.js` - 报修相关接口

### 17. UGC内容模块（新增）

**功能特性**：
- **帖子浏览**：
  - 帖子列表（分页加载）
  - 帖子详情查看
  - 点赞/取消点赞
  - 浏览数统计
- **评论系统**（新增）：
  - 评论列表展示
  - 发表评论
  - 回复评论
  - 评论点赞/取消点赞
  - 评论数统计
- **帖子发布**：
  - 支持纯文本、图片、视频、混合媒体类型
  - 图片/视频上传到OSS
  - 标题和内容编辑
  - 封面图设置
- **交互功能**：
  - 点赞互动
  - 评论互动
  - 浏览统计
  - 下拉刷新、上拉加载更多

**页面文件**：
- `pages/ugc/list.vue` - 帖子列表页面（广场）
- `pages/ugc/detail.vue` - 帖子详情页面（含评论功能）
- `pages/ugc/publish.vue` - 发布帖子页面（短视频平台风格UI）

**API接口**（`api/ugc.js`）：
- `fetchUgcList()` - 获取帖子列表
- `fetchUgcDetail()` - 获取帖子详情
- `createUgcPost()` - 发布帖子
- `likeUgcPost()` - 点赞帖子
- `unlikeUgcPost()` - 取消点赞
- `getComments()` - 获取评论列表
- `createComment()` - 发表评论
- `getCommentReplies()` - 获取评论回复
- `likeComment()` - 点赞评论
- `unlikeComment()` - 取消点赞评论
- `uploadFile()` - 上传图片/视频到OSS（在publish.vue中实现）

### 18. 消息通知模块（新增）

**功能特性**：
- **通知列表**：
  - 订单通知（订单状态变更）
  - 评论通知（收到新评论）
  - 点赞通知（帖子/评论被点赞）
  - 回复通知（评论收到回复）
- **未读提醒**：
  - 用户中心显示未读消息数量
  - 消息列表区分已读/未读状态
- **交互功能**：
  - 标记单条消息为已读
  - 标记所有消息为已读
  - 点击通知跳转到相关页面

**页面文件**：
- `pages/notification/notification.vue` - 消息通知列表页面

**API接口**（`api/notification.js`）：
- `fetchNotifications()` - 获取消息通知列表
- `getUnreadNotificationCount()` - 获取未读消息数量
- `markNotificationAsRead()` - 标记消息为已读
- `markAllNotificationsAsRead()` - 标记所有消息为已读

### 19. 个人资料模块（新增）

**功能特性**：
- **个人资料编辑**：
  - 用户名编辑
  - 性别选择（男/女）
  - 联系电话编辑
  - 个性签名编辑
  - 头像上传（上传到OSS）
- **UI设计**：
  - 独立的头像编辑区域
  - 表单式信息编辑
  - 实时更新用户信息

**页面文件**：
- `pages/userinfo/userinfo.vue` - 个人资料编辑页面

**API接口**（`api/member.js`）：
- `updateProfile()` - 更新个人资料
- `uploadAvatar()` - 上传头像到OSS

## 🚀 快速开始

### 环境要求
- Node.js >= 12.0.0
- npm >= 6.0.0
- HBuilderX（推荐）或 Vue CLI

### 安装依赖

```bash
npm install
```

### 运行项目

```bash
# 运行H5
npm run dev:h5

# 运行微信小程序
npm run dev:mp-weixin

# 运行App（需要HBuilderX）
# 使用HBuilderX打开项目，选择运行到手机或模拟器
```

### 构建项目

```bash
# 构建H5
npm run build:h5

# 构建微信小程序
npm run build:mp-weixin

# 构建App（需要HBuilderX）
# 使用HBuilderX进行打包
```

### 项目配置

**API地址配置**（`utils/appConfig.js`）：
```javascript
export const API_BASE_URL = 'http://localhost:8085';
```

**高德地图Key配置**（`utils/config.js`）：
```javascript
export const AMAP_KEY = process.env.AMAP_KEY || 'your-amap-key';
```

**应用配置**（`manifest.json`）：
- 应用名称
- 应用图标
- 启动页配置
- 权限配置

**页面路由配置**（`pages.json`）：
- 页面路径
- 页面样式
- TabBar配置

## 📱 页面路由说明

### TabBar页面
- **首页**：`pages/index/index`
- **广场**：`pages/ugc/list`（新增，原"帖子"）
- **购物车**：`pages/cart/cart`
- **我的**：`pages/user/user`

### 主要页面
- **商品列表**：`pages/product/list`
- **商品详情**：`pages/product/product`
- **订单列表**：`pages/order/order`
- **订单详情**：`pages/order/orderDetail`
- **创建订单**：`pages/order/createOrder`
- **登录**：`pages/public/login`
- **注册**：`pages/public/register`

## 🔧 核心功能实现

### 1. 网络请求封装

项目使用 `luch-request` 封装了网络请求：

```javascript
// utils/requestUtil.js
import Request from '@/js_sdk/luch-request/request.js'

const http = new Request()

// 请求拦截器：添加token
http.interceptor.request((config) => {
  const token = uni.getStorageSync('token')
  if (token) {
    config.header['Authorization'] = token
  }
  return config
})

// 响应拦截器：统一错误处理
http.interceptor.response((response) => {
  const res = response.data
  if (res.code !== 200) {
    uni.showToast({
      title: res.message || '请求失败',
      icon: 'none'
    })
    return Promise.reject(res)
  }
  return response.data
})
```

### 2. 状态管理（Vuex）

```javascript
// store/index.js
import Vue from 'vue'
import Vuex from 'vuex'

Vue.use(Vuex)

const store = new Vuex.Store({
  state: {
    hasLogin: false,
    userInfo: {}
  },
  mutations: {
    login(state, userInfo) {
      state.hasLogin = true
      state.userInfo = userInfo
      uni.setStorageSync('userInfo', userInfo)
    },
    logout(state) {
      state.hasLogin = false
      state.userInfo = {}
      uni.removeStorageSync('token')
      uni.removeStorageSync('userInfo')
    }
  },
  actions: {
    async getUserInfo({ commit }) {
      // 获取用户信息
    }
  }
})
```

### 3. 签到功能实现

```javascript
// pages/user/user.vue
import { signIn, checkTodaySignIn } from '@/api/signIn.js'

export default {
  methods: {
    async handleSignIn() {
      const response = await signIn()
      if (response.code === 200) {
        this.isSignedToday = true
        // 更新用户积分
        this.userInfo.integration = response.data.totalIntegration
        uni.showToast({
          title: response.data.message,
          icon: 'success'
        })
      }
    }
  }
}
```

### 4. 分类页面商品展示

```javascript
// pages/category/category.vue
export default {
  methods: {
    // 选择二级分类
    selectSubCategory(item) {
      this.currentSubId = item.id
      this.loadProducts()
    },
    // 加载商品列表
    async loadProducts() {
      const response = await searchProductList({
        productCategoryId: this.currentSubId,
        pageNum: 1,
        pageSize: 10
      })
      this.productList = response.data.list
    }
  }
}
```

## 🎨 UI优化说明

### 首页UI优化
- **顶部搜索栏**：透明背景，圆角卡片设计，点击跳转搜索页面
- **定位显示**：只显示省市信息，半透明卡片样式
- **轮播图**：优化指示器显示
- **快捷功能区**：4个快捷入口，渐变图标背景
- **模块化设计**：统一卡片样式，清晰的视觉层次
- **商品展示**：多种布局方式（横向滚动、网格、列表）

### 分类页面优化
- **两级分类展示**：左侧一级分类，右侧二级分类+商品
- **商品直接展示**：分类下直接显示商品，无需跳转
- **交互优化**：自动选中、状态提示
- **错误处理**：统一错误提示，使用logger工具

### 用户中心优化
- **签到功能**：美观的签到卡片，渐变背景
- **信息展示**：清晰的积分、成长值、优惠券统计
- **客服入口**：更新"联系客服"链接指向新客服页面

### 新增功能UI
- **报修页面**：集成高德地图，显示商家位置、距离、时间，支持导航
- **搜索页面**：搜索历史、热门搜索、搜索建议，输入框适配屏幕
- **客服页面**：智能客服界面，预设问题，关键词识别

## 📦 多端适配

### H5端
- 支持浏览器访问
- 支持响应式布局
- 支持定位服务（需要HTTPS）

### 微信小程序
- 需要配置小程序AppID
- 支持微信支付
- 支持分享功能

### App端
- 支持Android和iOS
- 需要配置原生插件
- 支持推送通知
- **地图功能**：使用原生map组件，支持高德地图导航
- **定位功能**：优化定位显示，只显示省市信息

## 🔐 用户认证

### 登录流程
1. 用户输入用户名和密码
2. 调用登录接口获取Token
3. 将Token存储到本地存储
4. 后续请求自动携带Token

### Token管理
```javascript
// 存储Token
uni.setStorageSync('token', token)

// 获取Token
const token = uni.getStorageSync('token')

// 清除Token
uni.removeStorageSync('token')
```

## 🐛 常见问题

### 1. 网络请求失败
- 检查 `utils/appConfig.js` 中的 `API_BASE_URL` 配置
- 检查后端服务是否启动
- 检查网络连接

### 2. 图片不显示
- 检查图片URL是否正确
- 检查图片服务器是否可访问
- 查看控制台错误信息

### 3. 登录后跳转失败
- 检查路由配置
- 检查权限验证逻辑
- 查看控制台错误信息

### 4. 签到功能不工作
- 确认后端服务已启动
- 确认已执行签到表SQL脚本
- 检查API接口地址配置

### 5. 地图功能不工作
- 检查高德地图API Key配置（`utils/config.js`）
- 检查`manifest.json`中的地图配置
- 确认已申请高德地图Key并配置正确

### 6. 报修功能不工作
- 确认后端服务已启动
- 确认已执行报修表SQL脚本（`mall_repair.sql`）
- 确认品牌表已添加地址和经纬度字段
- 检查高德地图API Key配置

## 📝 开发注意事项

1. **图片资源**：图片资源存放在 `static/` 和 `images/` 目录
2. **API接口**：所有API接口定义在 `api/` 目录
3. **组件使用**：优先使用uni-app官方组件
4. **样式规范**：使用rpx单位适配不同屏幕
5. **多端兼容**：注意不同平台的API差异

## 🚀 部署说明

### H5部署
1. 执行 `npm run build:h5`
2. 将 `dist/build/h5` 目录部署到Web服务器
3. 配置Nginx反向代理

### 微信小程序部署
1. 执行 `npm run build:mp-weixin`
2. 使用微信开发者工具打开 `dist/build/mp-weixin`
3. 上传代码到微信公众平台
4. 提交审核

### App部署
1. 使用HBuilderX打开项目
2. 配置App信息
3. 选择打包平台（Android/iOS）
4. 生成安装包

## 📄 许可证

MIT

## 🤝 贡献指南

1. Fork 本仓库
2. 创建特性分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 开启 Pull Request

## 📞 联系方式

如有问题或建议，欢迎提交 Issue 或 Pull Request。

---

**感谢使用 Mall App Web！** 🎉
